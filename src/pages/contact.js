import React from "react";

import styles from "./contact.module.css"

export default () => (
  <div>
    <h1 className={styles.title}>Contact</h1>
    <p className={styles.description}>Get in contact with me</p>
  </div>
)
